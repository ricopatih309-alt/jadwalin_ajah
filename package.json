{
  "name": "jadwalinajah",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev-frontend\" \"npm run dev-backend\"",
    "dev-frontend": "cd frontend && npm run dev",
    "dev-backend": "php -S localhost:8000 -t backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}